v0.0.9
------

* Supports fish shell (env.fish)

v0.0.8
------

* Add PREBUILT_PKG_URL_PATTERN to specific a custom mirror url to download prebuilt golang package.

v0.0.7
------

* Add option `--use-prebuilt` for command `oo get`, default to `false`.
* Downloads a prebuilt package from golang.org/dl for the first installation via `oo get`.
* Fix issues in `oo ls` and `oo` when no version installed.

v0.0.6
------

* Display a prefix `=>` for activated version in command `oo ls`.
